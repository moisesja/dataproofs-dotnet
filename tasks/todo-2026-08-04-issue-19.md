# Issue #19 — reject non-disjoint JWS headers + NetCrypto 1.4.0

Approved on 2026-08-04. This task fixes the verify-side RFC 7515 header-disjointness gap and
bundles the requested NetCrypto update in the same pull request.

## Scope and acceptance criteria

- [x] Reject a Flattened JSON JWS when its protected and unprotected header parameter-name
      sets overlap, regardless of whether the duplicate values match.
- [x] Reject the same overlap in every General JSON JWS signature entry.
- [x] Perform the structural rejection before signer resolution and signature verification.
- [x] Preserve conformant protected-only and unprotected-only `kid` handling, including the
      issue #10 requirement that an unprotected-only `kid` can resolve and identify the signer
      after successful verification.
- [x] Remove the obsolete protected-vs-unprotected `kid` value-agreement branch and update stale
      parser documentation that describes it.
- [x] Upgrade the centrally managed NetCrypto dependency from 1.1.0 to the latest stable release,
      verified from NuGet.org as 1.4.0 on 2026-08-04.
- [x] Update relevant changelog and version-sensitive documentation.
- [x] Keep source changes minimal and avoid public API changes.

## Implementation plan

- [x] Decode and inspect the protected header's actual JSON member names during JSON-signature
      extraction.
- [x] Enumerate an optional unprotected `header` object and throw `MalformedJoseException` on the
      first name shared with the protected header.
- [x] Share the invariant across Flattened and General extraction paths.
- [x] Add focused tests for identical duplicate `kid` and duplicate `alg` in both serializations.
- [x] Add or retain explicit regression coverage for protected-only and unprotected-only `kid` in
      both serializations.
- [x] Bump NetCrypto to 1.4.0 and restore/build against that package.

## Verification

- [x] Run the focused issue #19 regression tests.
- [x] Run all `DataProofsDotnet.Jose.Tests`.
- [x] Run the full solution build and test suite.
- [x] Run relevant repository gates for dependency hygiene and package output if available.
- [x] Ask one adversarial subagent to probe parser bypasses, exception ordering, General JWS edge
      cases, and NetCrypto-upgrade regressions; resolve every confirmed in-scope finding.
- [x] Inspect the final diff and working tree before commit.

## Review

- `JwsParser` now parses each protected JSON header once during structural extraction, retains its
  exact ordinal member-name set, and rejects any overlap with the unprotected header before calling
  the signer resolver or crypto provider. The now-obsolete both-present `kid` value comparison was
  removed.
- Flattened, General, and detached JSON paths share the invariant. Conformant protected-only and
  unprotected-only `kid` shapes continue to verify and report the signer.
- Adversarial review found and the implementation closed three adjacent structural gaps: mixed
  Flattened+General members, present non-object unprotected headers, and empty protected headers
  escaping as a raw `ArgumentException`. Re-review reported no remaining blocking findings.
- NetCrypto was verified as latest stable 1.4.0 through NuGet.org. All six source project assets
  resolve 1.4.0; the packed Core nuspec declares `NetCrypto` version `1.4.0`.
- Focused issue #19/hardening tests: 19 passed; full JOSE project: 374 passed; full solution: 855
  passed, zero failures and zero skips.
- Release build: succeeded with zero warnings and zero errors.
- AC-6 dependency hygiene: all six project closures and all six packed dependency graphs clean.
- Solution pack: all six `.nupkg` and `.snupkg` artifacts created successfully.
- `git diff --check`: clean. Final publication is waiting only on GitHub CLI re-authentication; no
  code, test, or review work remains.
