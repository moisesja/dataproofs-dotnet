<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <AssemblyName>SmokeJose</AssemblyName>
    <RootNamespace>DataProofsDotnet.Smoke.Jose</RootNamespace>
  </PropertyGroup>

  <!-- AC-10 clean-room form: `dotnet add package DataProofsDotnet.Jose` against the local feed.
       NetCrypto is the documented prerequisite (the signer substrate). See tasks/smoke/README.md. -->
  <ItemGroup>
    <PackageReference Include="DataProofsDotnet.Jose" Version="0.1.0" />
    <PackageReference Include="NetCrypto" Version="1.0.0-preview.1" />
  </ItemGroup>

</Project>
